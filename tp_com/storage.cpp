/*
    // auto storage class
    int x;
    auto int x; // the same, but can only be used w/in func
    
    // register storage class
    register int x; // stored in register instead of memory

    // static storage class
    static int x; // keep for the lifetime of the program

    // extern storage class
    extern void write_extern(); // ref of a global variable that is visible to ALL program files

    // mutable
*/